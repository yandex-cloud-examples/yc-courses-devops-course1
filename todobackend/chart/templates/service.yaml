apiVersion: v1
kind: Service
metadata:
  name: {{ include "todobackend.fullname" . }}
  labels:
    {{- include "todobackend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todobackend.selectorLabels" . | nindent 4 }}
